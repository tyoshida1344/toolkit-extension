正規表現生成タブの新規追加と、内部構造の改善をまとめたリリースです。

### Added
- 正規表現生成タブを追加 — 文字種や量指定子を選択して正規表現パターンをリアルタイム生成

### Changed
- 共通モーダルヘルパー `Toolkit.modal()` を導入し、モーダル表示を統一
- 状態永続化の宣言的ヘルパー `bindState` を追加し、入力値の保存・復元を簡潔に記述可能に
- ポップアップを開いた直後にスタイル未適用の状態が一瞬見える問題（FOUC）を改善
